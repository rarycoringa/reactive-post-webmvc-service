# Types

type User {
    id: String!
    name: String
    age: Int
    createdAt: String
}

type Post {
    id: String!
    content: String!
    user: User!
    createdAt: String!
}

# Inputs

input CreatePostInput {
    content: String!
    userId: String!
}

# Queries

type Query {
    getAll: [Post]!
    getAllByUserId(userId: String!): [Post]!
    getById(id: String!): Post
}

# Mutations

type Mutation {
    save(createPostInput: CreatePostInput!): Post!
    delete(id: String!): Boolean
}